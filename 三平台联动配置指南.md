# ✅ 三平台联动 - 完成状态

## 🎉 已顺利完成

| 项目 | 状态 |
|------|------|
| Gitee 仓库同步 | ✅ 已完成 |
| GitHub 仓库 | ✅ 正常 |
| 爱发电 API | ✅ 已配置 |

---

## ⚠️ 最后一步：上传工作流文件

由于 OAuth token 缺少 `workflow` scope，需要**手动上传**两个工作流文件。

### 操作步骤

#### 1. 打开 GitHub 仓库
https://github.com/Zeon7744/awesome-ai-short-drama

#### 2. 上传第一个文件

1. 点击 `.github` 目录
2. 点击 `workflows` 目录
3. 点击 **"Add file"** → **"Upload files"**
4. 创建新文件：`afdian-sponsors.yml`
5. 粘贴以下内容：

```yaml
name: 更新爱发电赞助者名单

on:
  schedule:
    - cron: '0 9 * * *'
  workflow_dispatch:

jobs:
  update-sponsors:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Fetch Afdian Sponsors
        env:
          AFDIAN_USER_ID: "0c59dda8a1bb11f19b9552540025c377"
          AFDIAN_TOKEN: "${{ secrets.AFDIAN_TOKEN }}"
        run: |
          TS=$(date +%s)
          PARAMS='{"page":1}'
          SIGN=$(echo -n "${AFDIAN_TOKEN}${PARAMS}${TS}${AFDIAN_USER_ID}" | md5sum | cut -d' ' -f1)
          
          RESPONSE=$(curl -s -X POST "https://afdian.net/api/open/query-sponsor" \
            -H "Content-Type: application/json" \
            -d "{\"user_id\": \"${AFDIAN_USER_ID}\", \"params\": \"${PARAMS}\", \"ts\": ${TS}, \"sign\": \"${SIGN}\"}")
          
          echo "$RESPONSE" | python3 -c "
import json,sys
data=json.load(sys.stdin)
if data.get('ec')!=200:
    print('API Error:',data.get('em'));sys.exit(1)
sponsors=data['data']['list']
lines=[]
for s in sponsors[:20]:
    u=s.get('user',{})
    lines.append(f\"- [{u.get('name','匿名')}](https://afdian.net/u/{u.get('user_id','')})\")
print('\n'.join(lines))
" > /tmp/sponsors.txt

      - name: Update README
        run: |
          SPONSORS=$(cat /tmp/sponsors.txt 2>/dev/null || echo "暂无赞助者")
          
          python3 << PYEOF
import re
content = open("README.md", encoding="utf-8").read()
sponsor_section = f"""## 🙏 感谢赞助

{SPONSORS}

---
"""
          pattern = r'## 🙏 感谢赞助\s*\n.*?(?=\n## |\Z)'
          if "## 🙏 感谢赞助" in content:
              content = re.sub(pattern, sponsor_section + "\n", content, flags=re.DOTALL)
          else:
              content = content.rstrip() + "\n\n" + sponsor_section
          open("README.md", "w", encoding="utf-8").write(content)
PYEOF

      - name: Commit and Push
        run: |
          git config user.email "action@github.com"
          git config user.name "GitHub Action"
          git add README.md
          git diff --staged --quiet || git commit -m "chore: update afdian sponsors"
          git push
```

6. 点击 **"Commit changes"**

#### 3. 上传第二个文件

重复上述步骤，创建 `sync-to-gitee.yml`：

```yaml
name: Sync to Gitee

on:
  push:
    branches: [main, master]
  workflow_dispatch:

jobs:
  sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Sync to Gitee
        env:
          GITEE_TOKEN: ${{ secrets.GITEE_TOKEN }}
          GITEE_USER: ${{ secrets.GITEE_USER }}
        run: |
          git remote add gitee https://${{ secrets.GITEE_USER }}:${GITEE_TOKEN}@gitee.com/${{ secrets.GITEE_USER }}/awesome-ai-short-drama.git
          git push gitee --all --force
          git push gitee --tags --force
```

#### 4. 配置 GitHub Secrets

1. 打开 https://github.com/Zeon7744/awesome-ai-short-drama/settings/secrets/actions
2. 点击 **"New repository secret"**
3. 依次添加三个 Secrets：

| Secret 名称 | 值 |
|------------|-----|
| `AFDIAN_TOKEN` | `WTcfMbUpkvFdACswPruY3ySxG87KJ45e` |
| `GITEE_USER` | `Zeon7744` |
| `GITEE_TOKEN` | `03be1ea3a7dbd34d1a7669426ea133d8` |

---

## 📋 三平台联动架构

```
┌─────────────────────────────────────────────────────────────┐
│                      用户访问入口                            │
│                                                             │
│   国内用户 ──→ Gitee ──→ 自动同步 ──→ GitHub                │
│                 ↑                                      ↓    │
│            爱发电赞助                              国际社区  │
│                 └──────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

| 平台 | 用途 | 链接 |
|------|------|------|
| **GitHub** | 国际开源社区 | https://github.com/Zeon7744/awesome-ai-short-drama |
| **Gitee** | 国内镜像加速 | https://gitee.com/Zeon7744/awesome-ai-short-drama |
| **爱发电** | 付费变现 | https://ifdian.net/a/Zeon7744 |

---

## 🔄 自动化流程

配置完成后，系统将自动执行：

1. **每日 9:00 (UTC+8)**：GitHub Action 拉取爱发电赞助者名单
2. **更新 README**：将赞助者列表添加到 `## 🙏 感谢赞助` 区块
3. **触发同步**：推送代码到 GitHub，自动同步到 Gitee

---

## ✅ 验证清单

上传工作流文件并配置 Secrets 后：

- [ ] GitHub Actions 页面显示两个 workflow
- [ ] 手动触发一次 `workflow_dispatch` 测试
- [ ] 检查 README 是否更新赞助者列表
- [ ] 验证 Gitee 与 GitHub 内容一致

---

完成以上步骤后，三平台联动体系就完全建好了！🎉
